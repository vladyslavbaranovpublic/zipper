# zipper

A file compressor tool written in C.

## Build

On Windows PowerShell:

```powershell
.\build.ps1
```

Or with `make`:

```sh
make
```

## Run

On Windows PowerShell:

```powershell
.\build.ps1 -Run
```

Or with `make`:

```sh
make run
```

The compiled executable is written to `build/`.
